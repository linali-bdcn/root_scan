扫描生成一份目录的树结构数据，根据这份数据进行处理。

1，勾选父级目录，连带勾选子级所有。若果取消父级则所有子级一并取消。
2，如果父级下所有子级被勾选，父级自动勾选，同时如果本带所有子级都勾选但有一个取消就连同父级一并取消，但不父级下其他子级不操作。
3，对树的操作记录一课树，构建一个及时跟新的树，当有父级文件被勾选，按照父级文件的文件路径先创建一棵树，如果该树已经有部分或全部存在，则创建没有的部分并插入到树中，如果父级文件下存在子级文件则将父级下所有子级构建一课新的树插入此时父级的路径节点下。
4，对数操作删除记录的树，对选择的父节点进行剪切，将该父节点下所有子节点的全部移除（直接将该父节点与其上级节点断开即可），然后对上级节点判断该节点是否还有子节点，有则执行想法2,没有则删除该节点重复判断其上级节点。

现在重新梳理程序逻辑，
概念：1树状目录结构(文件路径生成)2节点(文件作为)3子节点4父节点5被记录状态6子节点数量7生成树8减支9插入10叶子结点

开始  生成一课树根据文件路径生成几个节点
    再次选取一个新节点，根据文件路径再次寻找节点生成目录，标记时只生成不删除，只有最终会对没有标记的叶子进行删除。

    添加该节点所有子节点——>重复多次直到所有节点被添加(BFS)储存结构使用树的结构储存
    
    状态记录：当一个文件节点被标记后该节点的所有子节点均被标记(BFS传播,只标记未标记的节点)
             当一个文件节点被取消标记，该节点所有有标记的父节点取消标记(向上回溯直到检测到有没有标记的父节点)，同时对所有子节点也均取消标记(BFS传播只执行取消不判断.)
    分而处理   一份数据是扫描的原始数据   另一份是标记数据也是树结构数据
        节点的判断以扫描数据为基础，进行复制转移。
        当判断状态时对生成的树进行状态的判断。
    清理节点
        循环检查直到所有叶子结点都为标记的节点，不停删除状态为未标记的叶子节点，注：当叶子节点被删除时如果父节点成为了一个叶子节点则同样执行删除，循环就是为了这种情况。
    状态显示
        特点，及时渲染更新，每次隔多少秒刷新渲染，不直接展开显示而是折叠起来不渲染，等到点击某个节点展开该节点的1层子节点时渲染。
        勾选框，在每个节点前有个勾选框，被标记的，勾选框为✅️未勾选的是空的框。

发展方向：
叶子节点的状态统计
导出状态清单，为后续转移文件删除文件备份文件的功能做准备。

我需要你实现这套逻辑，重构项目UI，同时优化扫描逻辑，便于生成树。

存在问题:
1记录的逻辑中,子集中存在一个文件被取消标记时，本来因该父级传递状态取消全部标记的状态，但只有该被取消标记文件的父级被修改了状态，没有传递下去。
如：导出的：
{
  "metadata": {
    "exportTime": "2026/5/15 13:42:29",
    "totalSelected": 1,
    "totalSizeAllocated": "58.21 GB"
  },
  "items": [
    {
      "path": "D:/",
      "sizeBytes": 62506594899,
      "type": "dir"
    }
  ]
}
但其实：应该是这样的
{
  "metadata": {
    "exportTime": "2026/5/15 13:45:27",
    "totalSelected": 10,
    "totalSizeAllocated": "32.25 GB"
  },
  "items": [
    {
      "path": "D:/GameData/甜蜜女友",
      "sizeBytes": 10572070817,
      "type": "dir"
    },
    {
      "path": "D:/GameData/Counter-Strike 1.6",
      "sizeBytes": 510528019,
      "type": "dir"
    },
    {
      "path": "D:/GameData/植物大战僵尸杂交版v2.2",
      "sizeBytes": 94976491,
      "type": "dir"
    },
    {
      "path": "D:/Tools",
      "sizeBytes": 17822957651,
      "type": "dir"
    },
    {
      "path": "D:/data",
      "sizeBytes": 5680525116,
      "type": "dir"
    },
    {
      "path": "D:\\我的战争-[My War] 3.0.rar",
      "sizeBytes": 4760634206,
      "type": "file"
    },
    {
      "path": "D:/[lin]零的小游戏合集",
      "sizeBytes": 2688799161,
      "type": "dir"
    },
    {
      "path": "D:/项目程序",
      "sizeBytes": 1951866199,
      "type": "dir"
    },
    {
      "path": "D:/[前传]剧情地图",
      "sizeBytes": 979710253,
      "type": "dir"
    },
    {
      "path": "D:/ProgramData",
      "sizeBytes": 114256916,
      "type": "dir"
    },
    {
      "path": "D:/Superior RPG 至高RPG 1.4.2 客户端汉化By老本願",
      "sizeBytes": 27214171,
      "type": "dir"
    }
  ]
}

本来这个 是被取消的，但仍然被会被清理：
{
      "path": "D:/GameData/PCL2 mc",
      "sizeBytes": 17290156182,
      "type": "dir"
}
请你修改一下。

还有快捷键的方面，是否能够做到：
左键击文件展开或收起一层子集，
ctrl+右键 文件被标记或取消，

新功能：
  筛选统计：正则表达式，筛选文件名称类型大小等功能。
  筛选和统计，其中使用支持用正则表达式来匹配生成，弹出一个窗口用于专门的匹配筛选。同时预设一些筛选项，提供给没有正则基础的人快速使用，如匹配文件类型，文件大小范围，文件名称（可勾选也就是多个条件同时匹配）。另外添加一个可以对匹配到的文件在扫描目录中同步选择的功能并且专门记录一下选择信息方便用户找到目标文件后记录操作。
  统计。对目标进行统计操作，可选择多个对象目标，比如刚匹配的对象，或已经被选择的文件。然后统计各种文件类型的数量和总数量，占用空间的大小，平均单个文件占空间大小，以及一些其他有用的信息。 
  显示：匹配规则我认为有部分预设的匹配选项应该是勾选，并列出来。匹配名称，大小等可以让用户来，还有就是开一个正则表达式直接匹配的位置。匹配后的文件在新开的窗口下全部列出来，待用户勾选.生成一份记录表上面是匹配到的并且被用户选择的文件。然后用户可以在统计的那栏进行选择需要统计的信息也是勾选的。
  选择的文件大小统计:
  对文件采取累积式统计和总体遍历。
  累积式统计：每次操作进行加或减的操作，采用队列，以便不会出现数据统计错误，并且逐一对队列弹出。当选择某时添加该问价空间占用大小，计入总和。取消选择时，逻辑需要更加严密，因为有些文件夹可能子文件存在已经被取消选择的文件，则需用该文件的大小减去取消选择的子文件的大小，然后计入总和在总和中减去他。
  总体遍历:
  用户可以选择统计功能中的统计所有

转移,压缩,删除功能：
  1预测操作后的空间占用，过大则停止执行。
  2,压缩算法采用zip格式，另外压缩后将生成一份清单。
  3,每次执行操作都要产生日志记录，方便后续出事后进行数据恢复。
  4,删除操作时，如果删除过大则执行3多次警告后再进行删除操作因为数据过大将彻底删除，然后提醒用户如果有重要数据被误删请尽快将日志文件保存后找到专业人员恢复数据。
  5,删除操作采用循环遍历，速度要控制，让用户能及时停止操作。


